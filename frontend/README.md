# Frontend

Chart client for the crypto backtester platform (Phase 5+).

Planned stack: connect to `backend` REST (`/api/v1`) and replay WebSocket (`/ws/replay/{session_id}`).

API contract: [backend/docs/openapi.yaml](../backend/docs/openapi.yaml)
